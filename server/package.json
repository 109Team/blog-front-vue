{
  "name": "blog-front-server",
  "version": "1.0.0",
  "description": "blog front server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pm2 start server.js --name='front-server'"
  },
  "author": "hopen",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
